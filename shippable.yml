# language setting
language: node_js

# version numbers, testing against two versions of node
node_js:
  - 0.12.0

install:
  - npm install

# Running the tests with mocha
script:
  - npm run test-mocha
